# Runtime deps for the FastAPI backend + calc layer (production image).
# Streamlit-free and free of research-only deps (plotly, optuna, kdbcv, …).
# Pinned to the versions in uv.lock at the v0.1.0 release freeze (2026-08-14),
# so an image built later reproduces the released environment. If pyproject.toml
# changes, re-lock and re-pin these to match.
fastapi==0.141.1
uvicorn[standard]==0.52.3
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.0
umap-learn==0.5.12
hdbscan==0.8.44
numba==0.67.0
rich==15.0.0
zadu==0.4.2
# Key transitive deps pinned explicitly — these drifted between the lockfile and
# the image before (numba's JIT layer and ZADU's neighbour search):
llvmlite==0.49.0
pynndescent==0.6.0
faiss-cpu==1.15.0
joblib==1.5.3
